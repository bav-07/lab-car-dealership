const Car = function(manufacturer, model, price, engine){
    this.manufacturer = manufacturer;
    this.model = model;
    this.price = price;
    this.engine = engine;    
};

module.exports = {Car};